services:
  - type: web
    name: chatbot-flask
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --timeout 120 --preload"
    envVars:
      FRONTEND_URL: "https://YOUR-RENDER-APP.onrender.com"
